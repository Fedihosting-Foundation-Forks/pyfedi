name: Build and test
on:
  push:
    branches:
      - fhf-*

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/piefed
  SERVICE_NAME: piefed

permissions:
  contents: read

jobs:
  lint:
    name: Run linters
    runs-on: ubuntu-latest
    permissions:
      contents: read

    if: false  # upstream has no linting or formatting so this always fails

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          submodules: recursive
          show-progress: false
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.13"

      - name: Install the code linting and formatting tool Ruff
        run: pipx install ruff

      - name: Lint code with Ruff
        run: ruff check --output-format=github --target-version=py39

      - name: Check code formatting with Ruff
        run: ruff format --diff --target-version=py39
        continue-on-error: true # Piefed does not have a consistent code style

  test:
    name: Run tests
    runs-on: ubuntu-latest
    permissions:
      contents: read

    services:
      database:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: piefed
          POSTGRES_PASSWORD: password
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      redis:
        image: redis:6.2
        ports:
          - 6379:6379

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          submodules: recursive
          show-progress: false
          persist-credentials: false

      - name: Set up Python and dependencies
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.13"
          cache: pip
          pip-install: -r requirements.txt pytest

      - name: Initialize database
        env:
          FLASK_APP: pyfedi.py
          SERVER_NAME: localhost
          DATABASE_URL: postgresql+psycopg2://piefed:password@127.0.0.1/piefed
          PIEFED_DB_INIT_ADMIN_USERNAME: rimu # this is hardcoded in tests
          PIEFED_DB_INIT_ADMIN_EMAIL: admin@localhost
          PIEFED_DB_INIT_ADMIN_PASSWORD: thisIsN0tSecure.
        run: |
          set -x
          flask db upgrade
          flask init-db

      - name: Test with pytest
        env:
          FLASK_APP: pyfedi.py
          SERVER_NAME: localhost
          DATABASE_URL: postgresql+psycopg2://piefed:password@127.0.0.1/piefed
          HTTP_PROTOCOL: http
          PY_COLORS: "1"
        run: |
          pytest -rxXs --xfail-tb

  build-and-push-image:
    name: Build and push
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          submodules: recursive
          show-progress: false
          persist-credentials: false

      - name: Setup Git metadata
        id: git-meta
        run: |
          piefed_version="$(git describe --tags | sed 's|^v||')"
          echo "git-version=$piefed_version" >> "$GITHUB_OUTPUT"
          # injects version into app
          sed -E -i "s|^VERSION = '[^']*'\$|VERSION = '$piefed_version'|" app/constants.py

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ steps.git-meta.outputs.git-version }}
            type=sha,format=long

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4.0.0

      - name: Log in to the Container registry
        uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.repository_owner }}
          password: ${{ github.token }}

      - name: Build and push Docker image
        uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7.0.0
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: |
            ${{ steps.meta.outputs.labels }}
            com.datadoghq.tags.service=${{ env.SERVICE_NAME }}
            com.datadoghq.tags.version=${{ steps.git-meta.outputs.git-version }}
          annotations: ${{ steps.meta.outputs.annotations }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
